// src/app/splash/page.tsx
import Splash from "@/components/Splash"; 

export default function SplashPage() {
  return (
    <main>
      {/* Ici, il utilise automatiquement les valeurs par défaut définies dans Splash.tsx */}
      <Splash />
    </main>
  );
}